[package]
name = "global-crypto"
version = "0.1.0"
edition = "2021"
authors = ["You"]
license = "MIT OR Apache-2.0"
description = "Globally-callable cryptographic abstraction with rich types"
categories = ["cryptography", "no-std", "no-std::no-alloc"]

[dependencies]
critical-section = "1.2"
heapless = "0.8"

[features]
default = ["critical-section/std"]
